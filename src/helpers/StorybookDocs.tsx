import { Title, Primary, Controls, Subtitle, Description } from '@storybook/blocks';

const StorybookDocs = () => {
  // TODO: Review which blocks we want to use and in what order
  // TODO: Use this to define autodocs
  return (
    <>
      <Title />
      <Subtitle />
      <Description />
      <Primary />
      <Controls />
    </>
  );
};

export default StorybookDocs;
